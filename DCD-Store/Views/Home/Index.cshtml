<div class="container-fluid">
    @section hero{
        <div class="row" style="margin:10px">
            <div style="margin-top: 30px" class="col-12 text-center">
                <h1 class="display-4">Welcome to Our Classified Website</h1>
                <p class="lead">Here you can find all the latest classified ads for buying and selling goods and services. Browse through our categories to find what you're looking for.</p>
            </div>
        </div>
    }
    <div class="row">
        <div class="col-12 text-center">
            <h2 class="mt-5">Categories</h2>
            <div style="margin: 20px" class="row">
                @{
                    Html.RenderPartial("_Categories", "Electronics");
                }
                @{
                    Html.RenderPartial("_Categories", "Household");
                }
                @{
                    Html.RenderPartial("_Categories", "Cars");
                }
                @{
                    Html.RenderPartial("_Categories", "Bikes");
                }
                @{
                    Html.RenderPartial("_Categories", "Jets");
                }
                @{
                    Html.RenderPartial("_Categories", "Shoes");
                }
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-12 text-center my-5">
            <h2>Popular Ads</h2>
        </div>
        <div style="margin-bottom: 20px" class="col-12">
            <div class="height d-flex flex-row justify-content-around align-items-center">
                @{
                    Html.RenderPartial("_FeaturedItems");
                }
                @{
                    Html.RenderPartial("_FeaturedItems");
                }
                @{
                    Html.RenderPartial("_FeaturedItems");
                }
            </div>
        </div>
    </div>
</div>